Round 16 continues from the pushed round-15 state (remote main @ 212ba7f). The workspace had been reset, so the round opened with a fresh `git clone` and environment rebuild (`bun install`, `cp .env.example .env`, `db:generate` + `db:push`); the operating instructions (coding_agent_prompt.md) were internalized and the core docs re-read (AGENTS, CLAUDE, README, PAD v1.14, session_16, session_17, the round-15 plan). Baseline gates verified first: lint 0, tsc 0, 348/348 unit — the tree matched the documented Round-15 state exactly.

The round opened with the standard full live re-probe (agent-browser isolated sessions; the clone side against a fresh production build on :3000): login + the 9 views captured on both sides, then the established SORTED signature-multiset diff. The sorted diff found two structural deltas that did NOT classify into the documented buckets: the SyncedBadge class set (the live renders the Badge BASE only — no `border-transparent shadow hover:bg-primary/80` — while the clone rendered the default variant), and a wrapper-div delta around the Investments KPI row.

The decisive move this round: a SECOND diff pass with RAW-order signatures (tag|exact-class-attribute multisets — the sorted diff sorts class lists and is therefore structurally blind to class ORDER, exactly the trap AGENTS.md has warned about since round 8). The raw diff exposed a systematic set of sidebar/shell class-ORDER deltas on every view: the inner gradient pane, the sidebar-gradient container, the sidebar header block, the desktop nav (the round-8 pin had passed via the DRAWER nav string while the desktop nav drifted — a coverage gap), both bottom blocks, the nav icons (h-first `h-5 w-5` — an ADR-020 violation that slipped the dialog-forms h-first ban through TWO regex gaps: the dynamic `item.icon` tag and the ternary className), the logo icon color/size order, and the nav-pill attribute order (tabindex rendered before class). The live also now carries `transition-colors duration-300` on ALL 9 gradient panes — the round-4 "dashboard/expenses only" quirk is retired by the live update. Structural probes completed the picture: the dialog/chooser/FAB/Progress/mobile chrome all still match the round-14 pins, the Investments KPI cards are motion-WRAPPED on the live (a round-14 replication gap in the clone), MobileTopNav's extra wrapper div is not on the live, and the live's mobile drawer now mounts with a framer-motion slide-in (translateX −300px → 0 + fade, ~330ms spring, +35px overshoot — rAF-sampled; the drawer stays conditionally rendered, absent from the DOM when closed). Verdict: the LIVE UPDATED since round 15.

The round-16 remediation plan was written (docs/plans/2026-09-19-parity-remediation-round16.md) with six findings (F1 SyncedBadge, F2 Investments KPI wrappers, F3 MobileTopNav fragment, F4 the eight sidebar raw orders, F5 the pane order + quirk retirement, F6 the drawer slide-in) and validated against the codebase before execution — the F1 fix approach (variant="outline" + tail, with tw-merge stripping text-foreground) was verified byte-exact against the repo's own cn() BEFORE any implementation.

TDD execution — RED first: 11 new specs + 1 amended round-14 spec failed exactly as designed (the view-surfaces round-16 pins, the dialog-forms dynamic/ternary h-first ban extension, the functional-parity drawer-motion contracts), 347 pre-existing green. GREEN: all six findings implemented (sidebar.tsx carries F1/F3/F4/F6-style, investments-view.tsx the MotionWraps, finara-app.tsx the pane order, globals.css the `fin-drawer-in` keyframe hand-fit to the sampled frames; `entranceStyle` gained an animationName parameter, backward compatible).

Verification caught two environment failures worth recording: (1) the first post-fix clone re-capture STILL rendered the old badge — the "restarted" server had died to EADDRINUSE (the pkill missed the `next-server` process name), so a stale pre-fix server kept serving :3000; killing everything by the right process names and restarting cleanly resolved it. (2) The first E2E attempt failed every spec in 1-2ms — the workspace reset had wiped the Playwright browser cache (`Executable doesn't exist … chromium_headless_shell-1234`); `bunx playwright install chromium` fixed it, and a second transient run-death (detached process) cleared on retry.

Gates (final): lint 0 · tsc 0 · unit 359/359 (+11) · build clean · E2E 67/67 (3.3m). Post-fix verification on the fresh production build: BOTH diffs re-run against the same live captures — the SyncedBadge pair gone on every view, the MobileTopNav wrapper gone, Investments clean, and ZERO non-bucketed only-live raw signatures on 8 of 9 views (the Dashboard's sole remainder is the documented insight-empty-state data bucket). Every residual delta classifies into the documented buckets.

Deliverables beyond the fixes: `docs/screenshots/` (NEW — 11 dev-server captures: login, the 9 views, the mobile drawer); `.env.example` now documents `FINARA_INSIGHTS_LLM_OFF` (the env var the insights route and the E2E webServer read — found missing during the round's docs pass); README's stale architecture-table version (16.1.3 → 16.3.5) and its 334-count line corrected.

Documentation synced: the plan's §G execution record; AGENTS.md (the raw-order invariant + the SyncedBadge/MobileTopNav/drawer-motion pins, the motion invariant's round-16 additions, the corrected mobile-icon order note, the round-16 reference, counts 359); CLAUDE.md (the round-16 ANALYZE reference, the round-16 VERIFY lesson — pair the sorted diff with a raw-order diff, counts); README (counts, the round-16 contracts row, the env-var table row, the screenshots folder, the version fix); PAD v1.15 (header, §10 — the pane-quirk row retired, the round-16 resolved paragraph); this session log.
Round 16 complete — the live update since round 15 fully re-aligned (badge variant, eight sidebar raw orders, the pane order, the Investments motion wrappers, the mobile-nav fragment, the drawer slide-in), the raw-order diff discipline added to the permanent workflow, all gates green, documentation current.
