Round 13 continues from the pushed round-12 state (remote main @ a75b3b4). The repo was cloned fresh, the operating instructions (coding_agent_prompt.md) internalized, and the core docs re-read (AGENTS, CLAUDE, README, PAD v1.11, session_11, session_12, the round-12 plan).
Scandihaven (the tech-stack reference repo) was cloned and its AGENTS/scandihaven_SKILL reviewed — the financial-dashboard already mirrors the Next 16 / React 19 / Tailwind v4 CSS-first / strict TS / money-as-int-minor-units / Playwright-gates discipline.
Skills consulted via skills-catalog.md: clone-app-pat-pro (computed styles as ground truth, every-view recon), agent-browser (all live probing; isolated --session for the clone), tdd (red-green with pure-module seams).
Environment set up (bun install, db:push, dev on :3000). Baseline gates verified: lint 0, tsc 0, 275/275 unit — the tree matches the documented Round-12 state exactly.
Standing instruction for this round: the live app is the alignment target, and it had been updated — so the round began with a full re-probe. Logged in with the demo credentials and captured the 9-view DOM signature multisets for BOTH the live app and the clone (captures/r13/), then diffed them and chased every delta to ground truth.
The diff surfaced six real findings (everything else verified unchanged — login, shell, sidebar/mobile chrome, FAB, Quick Add step-2, AI Coach chat, Analytics controls, Settings, Import, bulk bar, KPI cards):
F1 (HIGH): AI Insights card completely redesigned — persisted TransactionInsight records (list "-created_date" limit 10, filter !is_dismissed, dismiss via update {is_dismissed:true}); new card anatomy (type icon + raw-id badge with per-type colors, dismiss X, h4 title, description, blue "💡 Suggestion:" callout, Confidence %/Category footer); Ask AI + rotating refresh; blank-while-loading; framer-motion row entrance.
F2 (HIGH): entrance animations REMOVED app-wide on the live (zero fade-in-up/stagger elements, zero CSS rules, animationName none on fresh load).
F3 (MEDIUM): full-modal dialog titles render DIVs (Add Expense/Add Income/AI Coach); the Quick Add chooser title stays h3.
F4 (MEDIUM): Quick Add chooser structure — plain overlay (z-40, no animate classes) > div.w-full.max-w-md wrapper > plain card; subtitle class order text-sm-first.
F5 (LOW): the Quick Add Amount label sits in a div.space-y-2 wrapper.
F6 (MEDIUM): Progress renders data-state=indeterminate with NO aria-valuenow; the fill positions via a manual translateX(-{100-value}%) (verified 24.5% -> -75.5% on the live).
Data deltas (documented bucket, no action): the live user deleted most records (1 income, 1 expense, 1 goal, 2 holdings, 1 budget @ $0, 0 insight records).
The round-13 remediation plan was written (docs/plans/2026-09-18-parity-remediation-round13.md) and validated against the codebase before execution.
TDD execution — RED first: 15 failing specs across insights.test.ts (new pure-module spec), functional-parity, view-surfaces, and ui-primitives, covering the route contracts, DTO shape, card anatomy, animation removal, dialog structure, and the Progress mechanism.
GREEN, in dependency order: the Insight Prisma model + AiInsightDto to the live field set; src/lib/insights.ts (deterministic draft builder, ≤3 records); the insights route rewritten as a pure list with generate-once-when-empty; the new PATCH /api/ai/insights/[id] dismissal; INSIGHT_TYPE_ICON/INSIGHT_TYPE_BADGE maps in ui-maps; the dashboard insights section rebuilt to the live anatomy (CSS entrance, prefers-reduced-motion gated — no framer-motion, per repo policy); fade-in-up/stagger stripped from every view + globals.css; dialog titles converted to DialogTitle-asChild DIVs; the chooser restructured (overlay > w-full max-w-md wrapper > plain card); the QA-amount label wrapper; the Progress primitive switched to the indeterminate + manual-transform mechanism with the Dashboard budget rows and Goals consuming it.
Browser verification caught a race: React StrictMode's double-effect fired two concurrent GETs — both saw an empty Insight table and both generated (duplicate rows). Fixed with the seed's atomic-marker pattern: a unique-key Setting row (_insights_generated) makes the generation right exclusive; the loser polls briefly for the records. Verified: 2 unique insights, no duplicates; dismiss + empty state + refresh re-list all live-exact.
The E2E gate then surfaced two more genuine defects — the round's deepest findings:
(1) Hermeticity break: the insights route's LLM polish rewrites record text nondeterministically. One full-suite run passed; the next failed the dashboard KPI spec because a polished insight title contained "Largest Expense Category" (substring-colliding with the KPI label assertion). Fix: the polish is env-gated (FINARA_INSIGHTS_LLM_OFF=1 pinned in the playwright webServer env; production leaves it unset) and the dashboard spec's label assertions hardened to exact:true.
(2) Join bug: the polish mapped confidence/category back onto drafts by TITLE — but the LLM is told to rewrite titles, so the lookup usually missed and silently fell back to 0.8/undefined. Fix: mergePolishedDrafts(drafts, polished) in the pure seam — a POSITIONAL join (the prompt pins order), dropping invalid entries without shifting later positions, capping at the draft count, carrying confidence/category/suggestedAction/type-fallback from the positional draft. 9 new red->green specs cover it.
The EUR spec was also corrected: persisted insight record text keeps its creation-time currency (live-faithful — the live's records are content, not live-rendered figures), so the app-wide $ sweep now excludes the insight rows (h4 ~ p — the dashboard's only h4s).
Final gates: lint 0 · tsc 0 · unit 311/311 (+36) · E2E 67/67 (+1) — two consecutive full-suite green runs confirming hermeticity.
Post-fix browser spot checks (dev build): insights row/badge/icon/dismiss-X/callout/footer classes live-exact; dismiss -> empty state -> refresh re-lists without regenerating; budget bars data-state=indeterminate + manual transforms; modal titles are DIVs; chooser overlay>wrapper>card structure exact; zero fade classes anywhere. Evidence: captures/r13/ (view sigs live+clone, modal sigs, screenshots, spot-check scripts).
Documentation synced: the plan's §G execution record; AGENTS.md (the insights persisted-record architecture, animation removal, Progress mechanism, dialog title rule, chooser structure, E2E additions); CLAUDE.md (round-13 ANALYZE reference, VERIFY additions incl. the flaky-E2E determinism lesson, counts); README (feature rows, counts); PAD v1.12 (header, §4.1 Insight model, §5.4 motion rewrite, §7 testing, §10 round-13 resolved row, §11 key files); this session log.
Round 13 complete — the clone re-aligned with the updated live app, all gates green, documentation current.
