Round 14 continues from the pushed round-13 state (remote main @ 51a2e5f). The workspace was reset, so the repo was cloned fresh; the operating instructions (coding_agent_prompt.md) were internalized and the core docs re-read (AGENTS, CLAUDE, README, PAD v1.12, session_13, session_14, the round-13 plan). Scandihaven was re-cloned for the tech-stack reference (patterns already mirrored) and the skills consulted via skills-catalog.md (clone-app-pat-pro's computed-styles-as-ground-truth, agent-browser for all live probing, tdd for the red-green loop).
Environment set up (bun install, db:push, dev on :3000). Baseline gates verified: lint 0, tsc 0, 311/311 unit — the tree matched the documented Round-13 state exactly.
Standing instruction honored: the live app is the alignment target — the round opened with a full re-probe. Logged in with the demo credentials and captured the 9-view + login signature multisets for BOTH apps, then chased every delta to ground truth with targeted evals: a per-element style-attribute audit (`main [style]` with opacity/transform), rAF computed-style sampling for the animation curves, MutationObserver frame logs for the stagger sequence, and dialog/chooser/FAB/Progress/login dumps.
The diff surfaced six real findings (everything else verified unchanged — shell, sidebar/mobile chrome, FAB anatomy, Quick Add step-2, AI Coach card, Analytics controls, Settings, Import, bulk bar, KPI cards, Progress indeterminate, dialog div-titles, the insights empty state, From/To inertness):
F1 (HIGH): the live still runs its framer-motion entrance system — round 13's F2 "entrance animations removed app-wide" was a MISDIAGNOSIS. The round-13 probe checked CSS `animationName` (always `none` for JS-driven animations) and the CSS classes (genuinely gone); the actual entrances animate INLINE STYLES on wrapper divs. 24+ wrappers across the 9 views: header rows, KPI cards, tiles, list rows, the Quick Actions panel — settled `style="opacity: 1; transform: none;"`, animating opacity 0→1 + translateY 20px→0 over ~330ms (spring, −2.3px overshoot), ~100ms stagger, on every mount; the refresh icons spin inside `transform: none` wrappers; the chooser overlay fades + its wrapper scales 0.9→1 on open.
F2 (MEDIUM): dialog overlays — live class order `fixed inset-0 bg-black/50 flex items-center justify-center p-4 z-50` (the clone's own doc comment already recorded it, the implementation diverged) + settled `overlayStyle`s on every overlay; Add Account carries `opacity: 1` only and its own order (z-50 mid-string).
F3 (MEDIUM): the chooser — header row `flex items-center justify-between mb-6` (mb-6 LAST), option buttons `px-4 py-2 w-full justify-start gap-3 h-12` (h-12 LAST), plus the open animation from F1.
F4 (MEDIUM): the login page was restyled — ten class-ORDER deltas (main, card, inner pad, stack, logo wrap, logo blur, h1, subtitle, divider span, bottom row), the Google label span-wrapped, and the field icons h-FIRST (`h-4 w-4` — a new exception to the app-wide w-first rule).
F5 (MEDIUM): the Expenses "Expense History" CardHeader — live renders CardTitle + Tabs as DIRECT children; the clone wrapped them in an extra `flex flex-wrap items-center justify-between gap-2` div.
F6 (LOW systemic): icon+text whitespace — the live renders NO whitespace text node between any icon and its text (probed incl. the mr-1 step-2 Add button and the Filters button + count badge); the clone's single-line JSX emitted leading spaces in 53 elements.
The round-14 remediation plan was written (docs/plans/2026-09-19-parity-remediation-round14.md) and validated against the codebase before execution (ViewHeader's three modes mapped, tw-merge order behavior verified with a live node test, the 53-instance sweep list, the dialog-call-site inventory).
TDD execution — RED first: 24 failing specs across functional-parity (the motion system, overlay contracts, chooser structure), view-surfaces (the expenses header, the whitespace sweep), login-view (the re-pins), and ui-primitives (the overlay base order); the round-13 animation-sweep test was amended (its src-map iteration caught the new globals.css entry's comment — the CSS-class sweep stays, the semantics note points at the round-14 correction).
GREEN, in dependency order: the three keyframe sets in globals.css under `@media (prefers-reduced-motion: no-preference)` (fin-card-in hand-fit to the sampled spring; fin-overlay-in; fin-scale-in); `entranceStyle` + `MotionWrap` in ui-bits (the settled inline style byte-for-byte + the inline animation; a missing keyframe list is a no-op so reduced-motion users render settled); ViewHeader's three modes all carry the entrance (the bare Import mode gained its missing MotionWrap); per-view delay maps (Dashboard's feature grid at 300–400ms BEFORE the tiles at 500–800ms, per the measured sequence; list rows capped at min(i, 8)×50ms — the live's uncapped stagger would take 9.8s on the 98-row seed, a documented adaptation); GradientCard threads a style prop; the dialog overlay base re-ordered + `overlayStyle`/`widthWrapperStyle` threaded to every call site (Add Account via the full-replacement merge); the chooser's orders + open animation; the login re-pin + the h-first exception in the dialog-forms guard; the Expenses header restructure; and the 53-instance whitespace sweep (multi-line JSX idiom).
Verification caught one more instance the sweep pattern missed: the Filters button rendered " Filters 2" (leading space + a space before the count badge) — the live renders `Filters` + `<span>2</span>` with zero whitespace nodes; fixed and the sweep test strengthened to ban the broader capitalized-component pattern.
Gates: lint 0 · tsc 0 · unit 334/334 (+23) · E2E 67/67 (the entrance animations do not affect Playwright actionability — opacity is not part of the visibility check). The E2E run needed the documented headless-shell install (session-14's known gap) and a clean restart after the documented Turbopack cache corruption + a port zombie.
Post-fix browser verification (production build): the clone's entrance animation trace matches the live's sampled curve (opacity 0→1 with −2.16px overshoot vs the live's −2.33px, same ~330ms span + stagger); the chooser overlay/wrapper styles and class orders live-exact; the full-modal overlays carry the settled styles; the Dashboard's three `transform: none` icon wrappers match; the login renders the re-pinned orders. Signature-multiset re-diff (fresh captures both sides): every remaining delta classifies into the documented buckets — seed-data counts, lucide-version internals (polylines/polygon), the insight empty state (live: 0 records), the demo-credentials affordance (login diff: 30 → 5, all five its elements), and the CSS-replication animation properties inside the style attributes (class-invisible to the signature walk).
Documentation synced: the plan's §G execution record; AGENTS.md (the motion-system invariant replacing the removal bullet, the overlay matrix, the login re-pin, the whitespace idiom + login h-first exception, the round-14 reference, counts); CLAUDE.md (round-14 ANALYZE reference, the animation-probing lesson, counts); README (the motion feature row, the E2E count fix 66→67, counts, the design-system motion paragraph); PAD v1.13 (header, §5.4 motion rewrite, §7.1, §10 round-14 resolved row, §11 key files); this session log.
Round 14 complete — the clone re-aligned with the live's motion layer and the five structural deltas, all gates green, documentation current.
