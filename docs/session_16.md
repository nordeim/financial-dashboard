Round 15 continues from the pushed round-14 state (remote main @ 8fe68c5). The workspace was intact, so the round opened with `git pull` (already up to date); the operating instructions (coding_agent_prompt.md) were re-internalized and the core docs re-read (AGENTS, CLAUDE, README, PAD v1.13, session_14, session_15, the round-14 plan). The scandihaven reference and the skills catalog were consulted in round 14 and re-checked this round via the round-plan chain (patterns unchanged; clone-app-pat-pro's re-probe-first discipline and agent-browser for all live probing; tdd for the red-green loop).
Baseline gates verified first: lint 0, tsc 0, 334/334 unit — the tree matched the documented Round-14 state exactly. The production server from the round-14 E2E gate was still serving the pushed build on :3000 (used as the clone side for the re-probe).
Standing instruction honored — the round opened with the full live re-probe. Logged in with the demo credentials and captured the 9-view + login signature multisets for BOTH apps, then ran the targeted structural probes: a per-element style-attribute (motion) audit on the Dashboard, the Add Expense modal overlay/card/title, the FAB chooser (overlay/wrapper/card/header row/close/option buttons), the Goals Progress indeterminate mechanism, the Analytics controls, and the mobile-viewport chrome.
The verdict: THE LIVE APP IS UNCHANGED SINCE ROUND 14. The signature diff was semantically identical to the round-14 post-fix baseline — the same per-view delta counts (52/25/26/26/18/0/3/34/3), the same signatures, the same counts, every one classifying into the documented buckets (seed-data counts, lucide internals, the insight empty state, the demo-credentials affordance, the CSS-replication animation properties). The login diff is exactly the five demo-credentials elements. The live's Dashboard still carries its 19 settled motion elements; the clone's 27 is fully data-explained (3 budget rows vs 1, 6 activity rows vs 2) with identical structure and settled styles. The dialog overlay, chooser, Progress, Analytics controls, and mobile chrome all probe live-exact. Visual parity held with nothing to remediate.
The round therefore pivoted to the codebase's own production-readiness gaps. A `bun audit` pass found 90 advisories (3 critical, 48 high) with the criticals in the RUNTIME set: next 16.1.3 (GHSA-2xp9-vwfh-vxw4 + GHSA-p293-qw3h-jr36 unauthenticated RCEs + GHSA-955p-x3mx-jcvp disclosure), next-auth 4.24.11 (GHSA-7rqj-j65f-68wh critical + a high) — plus a high in sharp (GHSA-f88m-g3jw-g9cj, libvips CVEs), moderates in next-intl and uuid, and framer-motion sitting in the manifest in contradiction to the documented no-framer-motion policy. Import-count verification: next-auth, next-intl, framer-motion, and uuid have ZERO imports across src (including the vendored ui primitives), e2e/, and config files — base44-scaffold leftovers. The PAD §10 also still carried the open MEDIUM "no migration history" item.
The round-15 remediation plan was written (docs/plans/2026-09-19-parity-remediation-round15.md) and validated against the codebase before execution (vitest include patterns, the CI frozen-lockfile constraint, the Playwright webServer's db:push dependency, peer-dep compatibility for the framework bump, next's own sharp optional-range interplay — the 16.2 security line was rejected precisely because next@16.2.x pins `sharp ^0.34.5`, inside the vulnerable range, which would nest a second vulnerable copy; next@16.3.5 pairs with ^0.35.4 natively).
TDD execution — RED first: 10 failing specs across the two new contract files (manifest-contracts: the next advisory floor, the sharp floor, the banned-dependency set, the lockfile ban; migrations-baseline: the lock file, the migration set, the per-model CREATE TABLE drift guard). GREEN: next 16.1.3 → 16.3.5 + eslint-config-next 16.3.5 in lockstep + sharp ^0.35.4; the four planned removals (next-auth, next-intl, framer-motion, uuid) PLUS a follow-on find from the audit trace — @mdxeditor/editor, react-syntax-highlighter, and @reactuses/core, the same zero-import class whose transitive trees carried advisories (prismjs/lexical via mdxeditor, js-cookie via @reactuses) — seven removals total, with the banned list extended to all seven in the manifest spec. The migration baseline: prisma/migrations/20260919000000_init/migration.sql generated via `prisma migrate diff --from-empty` + migration_lock.toml + the db:deploy script, verified baseline ≡ schema (migrate deploy on a scratch DB, then db push reports "already in sync").
Verification caught an environment failure worth recording: the first E2E attempt crashed Chromium (`browserContext.newPage: Target crashed`) — the four agent-browser Chromium instances from the live-probing phase were still resident on the 4GB machine. Closing the probe sessions freed 1GB+ and the gate ran clean — the round-14 resource lesson extended: close probe sessions BEFORE the gate, not just dev servers.
Gates (final): lint 0 · tsc 0 · unit 348/348 (+14) · E2E 67/67 TWICE (after the framework bump and again after the removals) · build clean · `bun audit` 90 → 43 findings with 0 critical (the remainder all dev/CLI-time transitive trees — eslint/vitest tooling, tailwind's postcss, the prisma CLI's effect — accepted and recorded in the PAD §10). Post-upgrade browser verification on the fresh production build: all 9 view signature captures BYTE-IDENTICAL to the pre-upgrade files (zero rendering delta from the framework bump — the strongest possible evidence the upgrade changed nothing user-visible), the motion system intact, the chooser opening with the live-exact overlay + animation.
Documentation synced: the plan's §G execution record; AGENTS.md (the dependency-hygiene + migration-baseline invariants, the db:deploy row, the round-15 reference, counts 348); CLAUDE.md (the round-15 ANALYZE reference, counts); README (the round-15 contracts row, the dependency-posture security bullet, the db:push/db:deploy workflow note, counts); PAD v1.14 (header, §4 migrations rewrite, §7.1 distribution, §10 — the migration row Resolved, the audit-tail acceptance row, the round-15 resolved paragraph); this session log.
Round 15 complete — parity verified as holding (zero live drift), the runtime dependency surface cleaned to zero critical advisories, the migration baseline adopted with a drift-guard spec, all gates green, documentation current.
