Round 20 continues from the pushed round-19 state (remote main @ 8488f5c + 9feca42, the round-19 session-log follow-up). The workspace survived this time — `git pull` fast-forwarded to 9feca42 (docs/session_25.md, the round-19 raw session stream), the environment was intact (node_modules, .env, db/custom.db), and the known `DATABASE_URL` shadow trap was handled with `unset` on every invocation. The baseline gates opened green: lint 0 · tsc 0 · 392/392 unit — matching the documented round-19 state exactly.

The operating instructions were internalized (the repo skills catalog — clone-app-pat-pro / agent-browser / tdd — and the scandihaven tech-stack reference, institutionalized since round 18). The core docs were re-read (AGENTS, CLAUDE, README, PAD v1.18, session_24, session_25, the round-19 plan) and the codebase alignment validated: 17 finara component files + theme.ts, 17 unit files / 392 specs, 11 E2E files / 67 specs, the round-19 seam (`routeDescription` in route-metadata.ts) verified in place.

The round opened with the standard full live re-probe (agent-browser isolated sessions; the clone side against a fresh production build on :3000, both sides dark-themed): login + the 9 views + login-on-logged-out captured on both sides (10 surfaces + login), then BOTH diffs (sorted + raw), the structural probes, a live-r19-vs-live-r20 drift diff, and a 5-pair VLM side-by-side sweep. Verdict: **the live is UNCHANGED on every layer — the first zero-drift head round since 16.** The live-r19-vs-live-r20 diff is byte-identical on 10 of 11 surfaces (Analytics differs only by 18 `recharts-dot` circles + 3 wrapper groups — the live user's trend-line data shrank; a pure data-bucket delta); the clone-vs-live diff is IDENTICAL to the round-19 diff on 10/11 surfaces (both modes), RAW==SORTED everywhere, and every structural probe matched its pin: the motion audit (19 settled on the live Dashboard — the live user's data grew back to the round-14 count), the Add Expense modal (overlay/card/div-title exact), the FAB chooser (z-40 overlay + `w-full max-w-md` wrapper + plain card + "Quick Add" / "What would you like to add?"), the Progress indeterminate mechanism (translateX −75.5%, no aria-valuenow), the mobile chrome (the drawer slide-in rAF-sampled at −300px → +35.6px overshoot → settle; 2 navs / 18 links document-wide; the `p-4 border-t border-slate-700/30` bottom), the SyncedBadge (base-only), the sidebar raw orders, and the user menu (2 items, span-wrapped). The HTTP-layer audit — the standard opener since round 17 — found the head UNCHANGED for the first time in four rounds: the per-route description template (all three metas), the login-only viewport/theme-color/sized-icon-pair/og-descriptor/twitter:image:alt, the dashboard-canonical→root quirk, robots (wildcard shape), sitemap (the 9-URL set), and the security-header trio all byte-exact vs the round-17/18/19 pins. The VLM sweep returned EQUIVALENT on Investments/Analytics/Goals; Dashboard and Expenses initially flagged on full-page scaling artifacts (the clone's 96-row Expenses page is far taller, so the scaled-down comparison reads as a "narrower sidebar" — the measured sidebar is exactly 256px on both sides, texts unwrapped, byte-identical widths) — the viewport-only re-comparisons returned EQUIVALENT. Net: 5/5.

Zero parity drift pivoted the round to the codebase's own production-readiness gaps (the round-15 precedent). The audit found the base44 scaffold's dead tail still shipping: 31 vendored-but-unused shadcn primitives in src/components/ui/ (a closed cluster — zero imports from app code, the pinned set, or any spec; the only cross-references are within the dead cluster itself) and 34 dead runtime dependencies (9 zero-import — including the look-alikes @tanstack/react-query, date-fns, zod, zustand, @tanstack/react-table — plus 25 imported only by the dead files: 15 unrendered Radix primitives, cmdk, embla-carousel-react, input-otp, next-themes, react-day-picker, react-hook-form, react-resizable-panels, sonner, vaul; prisma/react-dom/sharp verified as false positives and kept). Two doc-drift finds completed the list: the AGENTS ADR-015 pinned-set enumeration names `separator` but not `dialog` (the suite pins the reverse), and the README says 16 app components (17 + theme.ts is the truth).

The round-20 remediation plan was written (docs/plans/2026-09-19-parity-remediation-round20.md) and validated against the codebase BEFORE execution — the breakage surface confirmed to be exactly manifest-contracts.test.ts (no other spec or E2E references the dead files; the alive 17 primitives are untouched by the deletion set).

TDD execution — RED first: the round-20 specs (34 package.json exclusions + the lockfile-absence table + the ui-inventory contract) failed exactly as designed — 36 failing / 392 passing. GREEN: both findings implemented (the 31 files deleted; `bun remove` on the 34 packages — the runtime set drops 58 → 24, every remaining package imported by app code). One empirical correction during GREEN: `@radix-ui/react-separator` was assumed to stay transitively (via the kept dropdown-menu) but FULLY left the tree — the vendored dropdown-menu renders its separator through the dropdown-menu package itself — so the lockfile pin was tightened to include it.

Post-fix verification on the fresh production build: the body-DOM signature diff pre-fix vs post-fix is BYTE-IDENTICAL (525/525 root, 525/525 dashboard, 268/268 goals, both modes — nothing app-rendered moved); the HTTP-layer pins re-verified (/Goals description template, /login viewport/theme-color/icon-pair/og+twitter:image:alt); `bun audit` unchanged (43 findings, 0 critical — the accepted dev/CLI tail; the removed packages carried no runtime advisories).

E2E incident + recovery: the first run failed massively (specs at 1-2ms across all files) — not a code regression: the probe phase had left ~66 chromium processes across the seven agent-browser sessions alive, exhausting the sandbox's process/memory budget so Playwright's browser contexts could not launch. All sessions closed (memory 1.8Gi → 430Mi used), the clean re-run went green 67/67. (Lesson recorded: close agent-browser sessions before the E2E gate — the sandbox's 3.9Gi budget does not fit the probe fleet + the E2E runner together.)

Gates (final): lint 0 · tsc 0 · unit 428/428 (392 + 36 net-new) · build clean · E2E 67/67. Deliverables beyond the fixes: AGENTS/CLAUDE/README/PAD v1.19 aligned (the ADR-015 pinned-set correction, the round-20 references, counts 392 → 428, the README component-count 16 → 17 + theme store + the stale __tests__ 311 → 428, the Security § dependency-posture extension, PAD §7 per-file counts refreshed + site-metadata finally listed, §10 round-20 resolved row, §11 manifest-contracts seam entry); docs/screenshots/ refreshed (dev-server captures of the remediated app); .env.example re-verified aligned (no new env vars).

Round 20 complete — the live re-verified at zero drift on every layer (the head audit stays in the standard opener; four rounds of head movement paused this round, the body DOM has been stable since round 17), and the codebase's scaffold dead tail cleared: the ui inventory is now exactly the 17 app-rendered primitives and the runtime dependency set is 24 packages, all pinned both directions by the extended manifest contracts. The standing lesson: when the live probe finds zero drift, the round pivots to production-readiness — round 15 set the precedent, round 20 completed the sweep.
