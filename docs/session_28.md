Round 21 continues from the pushed round-20 state (remote main @ 772fe83, plus the two follow-ups 9e24a9e session-log and 9f0882c env-example). The workspace had been reset again — the repo was re-cloned over HTTPS, both environments (node_modules, .env, db/, playwright browsers) rebuilt from scratch. The known `DATABASE_URL` shadow trap (the sandbox exports an absolute `file:/home/z/my-project/db/custom.db`) was confirmed active and handled with `unset` on every server/gate invocation.

The operating instructions were internalized (the uploaded coding-agent prompt — Mode C audit → Mode A/B remediation, evidence-backed claims, TDD, no guardrail weakening; the repo skills catalog — clone-app-pat-pro / agent-browser / tdd / the Tailwind v4 pair (nextjs16-tailwind4 + tailwind-patterns) / distill-codebase-skill + to-distill-project-into-skill). The core docs were re-read (AGENTS, CLAUDE, README, PAD v1.19, session_26, session_27, the round-20 plan) and the codebase alignment validated: 17 finara components + theme.ts, 17 pinned ui primitives, 24 runtime dependencies, 17 unit files / 428 specs, 11 E2E files / 67 specs — matching the documented round-20 state exactly.

The round opened with the standard live re-probe (agent-browser isolated sessions; the clone side against a fresh production build on :3000), with the operator's two explicit focus areas layered on: the mobile navigation menu and possible Tailwind v4 bugs. Verdict: **the live is UNCHANGED on every probed layer.** The dashboard body-DOM signature diff carries zero non-bucketed deltas — the live user has dismissed all AI-insight records (the live renders the round-13 empty state), and the activity/budget row counts and chart/icon inner-stroke populations differ with the data. The mobile drawer is byte-identical on both sides (`lg:hidden fixed inset-0 z-40 bg-slate-800 dark:bg-gray-900`, the `pt-20` wrapper, the compact pills, the `p-4 border-t` bottom with the direct Sign Out) and works end-to-end on BOTH sides: open, nav-link click navigates AND unmounts the drawer, the Menu↔X glyph swaps both ways, zero console errors. The Tailwind v4 audit is clean in dev AND production: the bundle carries `lg\:hidden{display:none}`, `.bg-slate-800`→var, `.dark\:bg-gray-900:is(.dark *)`, and the `fin-drawer-in` keyframes; no dynamic class construction exists in the finara components; and the unlayered v3 palette pin wins the cascade over Tailwind v4.1.18's regenerated default ramp AND its `@supports lab()` re-definition (computed `rgb(30, 41, 59)` = v3 `#1e293b` on the live-exact drawer surface). The HTTP layer matches every round-17/18/19/20 pin byte-exact (the `Goals on Finara. …` description template on all three metas; the login-only viewport-fit/theme-color/sized-icon-pair/twitter:image:alt; the security-header trio + the clone's hardening pair). One false alarm during the audit: the CI workflow's `branches: [main]` initially read as corrupted in terminal output — the YAML parses and the raw bytes are clean (the machine-readable diagnostic wins over the noisy terminal, exactly as the operating instructions require).

Zero parity drift pivoted the round to the environment/tooling gaps the operator prompt pins (the round-15/20 precedent). The audit found: the committed `.env.example` (updated at 9f0882c) documents a database-path contract that did not exist — `src/lib/db-path.ts`, its spec, plus three phantom references (`bun run db:seed`, `docs/DEPLOYMENT.md §4`, `tests/db-path.test.ts`); the runtime resolution depended on the process CWD (verified working for the standard repo-root flows — CLI `db:push`, `next dev`, `next start` all touch `<repo>/db/custom.db` — but not guaranteed for other launch directories); and the fresh environment needed the Playwright chromium headless-shell rebuilt before the E2E gate could run at all (the first baseline attempt failed 67/67 at 1-2ms on the missing executable — the round-19 environment-lesson class, not a code regression).

The round-21 remediation plan was written (docs/plans/2026-09-22-parity-remediation-round21.md) and validated against the codebase BEFORE execution: DATABASE_URL's only consumer is the Prisma datasource (the wiring point is `src/lib/db.ts`); Prisma 6.19.2 supports the `datasourceUrl` constructor option (verified in the generated runtime); the E2E webServer's absolute-URL pin must pass through unchanged (hermeticity preserved by design).

TDD execution — RED first: `src/lib/__tests__/db-path.test.ts` (six contract cases, the repo-root anchor derived from the spec module's own location so the assertions cannot be tautological) failed on the missing module with all 428 existing specs green. GREEN: `src/lib/db-path.ts` implemented (walk-up from the process CWD to the nearest `prisma/schema.prisma`, CWD fallback, absolute/PostgreSQL passthrough, the crisp missing-env error) and wired into `db.ts` through `datasourceUrl`. One GREEN-phase correction: the all-optional `DatabaseEnv` interface tripped TS2559 against Node's index-signature `ProcessEnv` — fixed by carrying the index signature on the interface (no casts). Suite: 434/434.

Runtime verification: the production server from the repo root serves the seeded settings envelope with `<repo>/db/custom.db` the only DB file touched; the subdirectory-launch probe (process CWD in `scripts/`, relative URL) resolves to the same absolute target — the CWD-independence contract, executed; the missing-env path throws the actionable `.env.example`-pointing message; the E2E webServer's absolute `db/e2e.db` still isolates (the passthrough contract).

Gates (final): lint 0 · tsc 0 · unit 434/434 (428 + 6 net-new) · build clean · E2E 67/67 on the post-change re-run. Deliverables beyond the fixes: `.env.example` aligned (the real test path, the real auto-seed flow, the real deployment note); AGENTS/CLAUDE/README/PAD v1.20 aligned (the db-path invariant + round-21 references + counts 428 → 434); `financial-dashboard_SKILL.md` distilled (the to-distill-project-into-skill meta-skill applied — 20 sections + appendices); docs/screenshots/ refreshed (dev-server captures of the remediated app); this session log; the round-21 plan §G.

Round 21 complete — the live re-verified at zero drift on every probed layer (with the mobile-navigation and Tailwind v4 focus areas explicitly cleared, dev and production), and the environment/tooling layer the operator prompt targets is closed: the database-path contract exists and is pinned, the `.env.example` matches the codebase, and both test suites are verified green on the fresh environment. The standing lessons: rebuild the Playwright browsers after a workspace reset before trusting an E2E failure, and trust machine-readable diagnostics (the YAML parser, the computed styles, the raw bytes) over terminal rendering every time.
