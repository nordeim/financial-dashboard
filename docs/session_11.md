Resuming from the interrupted session. Round 11 was fully delivered and pushed (remote main @ 4031185 + session log 26290be), so this session is a new parity round. Per the standing instruction: `git pull`, then review the core docs (AGENTS, CLAUDE, README, PAD, session_9, session_10, round-11 plan) against the codebase.
A new remote commit added `docs/session_10.md` after the round-11 push. All core docs reviewed: rounds 2–11 drove the clone to verified parity across static rendering, real routes, functional behavior, theme lifecycle, and interactive states; PAD §10's top remaining items are the **Playwright E2E layer** (MEDIUM), **CI pipeline** (HIGH), and the dead `tailwind.config.ts` (LOW). Session logs 9 and 10 both recommend the E2E layer as the natural next round. The repo-included `skills/` catalog (playwright-cli, e2e-testing-lessons, testing-patterns) directly informs the design.
Environment verified: Playwright 1.62.1 is available with a cached chromium build (offline-capable); the dev DB from the previous session survives at the parent-dir location with pristine seed counts (96/4/4/3/8/3). Baseline gates: lint 0 · tsc 0 · **270/270** · build 0 — codebase matches the documented Round 11 state.
**Round 12 scope fixed**: quality-infrastructure round — (1) F0 live probe of the last unprobed functional corner (exports under EUR + non-US date-format settings, feeding E2E export specs), (2) the Playwright E2E golden-path layer, (3) CI workflow, (4) `tailwind.config.ts` cleanup, then docs + atomic commits + push. Plan written to `docs/plans/2026-09-17-parity-remediation-round12.md`.
Dev server started (known Turbopack cache issue hit once — reset `.next` and restarted per the documented procedure). Clone export implementation studied: `/api/export` and the GDPR route never read settings — the probe question is whether the LIVE export follows them.
**F0 probe (live)**: logged in, confirmed default settings (USD / MM/dd/yyyy), patched XHR/Blob/anchor to capture the live export request and generated file. The live builds its CSV **client-side** from 3 entity GETs (no server export endpoint). Default-settings CSV captured — matches the clone exactly (ISO date-only for expenses, microsecond timestamps for income/investments, raw decimals, `financial-report-<ISO date>.csv` filename). Switched live settings to EUR + dd/MM/yyyy, re-exported: **byte-identical CSV** — dates stay ISO (NOT dd/MM/yyyy), amounts stay raw decimals (no €), filename keeps the ISO date. GDPR JSON probe: same verdict — settings-independent, snake_case shape, ISO dates, raw decimals. Settings restored to defaults and verified persisted; theme left dark as found. **F0 verdict: live exports are settings-independent; the clone is already live-exact. Zero code change — pin it in E2E.**
E2E infrastructure built: `playwright.config.ts` (production build on :3100, `reuseExistingServer: false`, hermetic `db/e2e.db` via an absolute-path `DATABASE_URL` in the webServer env, `rm -f` + `prisma db push` reset per run, `ensureSeeded()` fills it, serial `workers: 1`, `retries: 0`), `e2e/helpers.ts` (login/signOut via the real UI, native-dialog accept, unique-name factory, shadcn Select driver, download readers), `package.json` scripts `test:e2e` / `test:e2e:headed`, devDependency `@playwright/test@1.62.1`, `.gitignore` + eslint ignores for the artifact dirs.
Auth spec written first (route contracts from ADR-021) — smoke run surfaced the first real finding: on an unauthenticated deep link, the clone renders the login form but **keeps the deep-link title** ("Goals | Finara"). Live probe with a fresh logged-out browser session: the live title on unauth redirect is bare **"Finara"**. Second delta found while at it: live `/login` title is also bare **"Finara"** — the clone's `Login | Finara` (round-8's plan had left the login title unprobed). Live 404 title re-verified: round-8's pin is correct.
**Title fix (TDD)**: RED specs first (view-surfaces route pin + functional-parity round-12 block). GREEN: `parseRoute("/login")` title → "Finara"; a render-time route adjustment in FinaraApp for logged-out deep links — reading the **LIVE session store** (`readSessionCache()`), because the hydration render snapshot is always session-less and trusting it would bounce authenticated visitors off their deep links (the same discipline the redirect effect documents); SSR-guarded with a `window` check. The redirect effect gains a `route` dep so browser-back while logged out re-gates to /login.
**The title fix didn't land at first** — dev-mode and production both kept showing "Goals | Finara". Debugged systematically: render-phase `document.title` write → silently lost. First-macrotask write → also lost. A `document.title` setter trap installed at module load caught only my own writes — so the reset bypasses the setter. A load-time **MutationObserver** caught the culprit: **React hydrates the `<title>` node's textContent AFTER client effects run**, clobbering any client-side title correction (the live serves the deep-link title in raw HTML too — its client rewrite just wins because it doesn't keep `<title>` in the React tree). Final fix: a post-commit title effect guarded by a MutationObserver that restores `route.title` on any external write (converges — equal title → no-op). Verified against the production build.
Writing the remaining specs (~66 total across 11 files — accounts+investments and settings+theme and mobile+console consolidated into shared files). Mid-writing, a second genuine delta surfaced: **the import route still rejected negative `amountMinor`** (rows mode guard `amountMinor < 0`) and the finara-export normalizer rejected negative decimals — an ADR-024 layer round-11 missed (the import route wasn't in its route list). RED specs → GREEN: both relaxed to integer-shape-only.
**Third genuine delta: the FAB toggle.** The quick-add spec's "FAB click closes the chooser" failed — physical clicks landed on the FAB but the chooser stayed open. Traced to TWO stacked causes: (1) Radix's modal lock sets `pointer-events: none` on `<body>`, deadening the z-50 FAB while the chooser is open — but a live probe (elementFromPoint at the FAB center, chooser open) proved the live's FAB STAYS clickable and acts as the close toggle (the live's chooser is a plain div in the app tree, no portal); the live's full modals DO cover the FAB (overlay z-50) — so only the chooser changes. (2) After switching to `modal={false}`, Radix's outside-interaction handling (outside pointerdown AND outside focus — the FAB takes focus on mousedown) dismissed the chooser before the FAB's click-toggle ran, re-opening it: a traced close→toggle double fire. Fix: `modal={false}` + `onInteractOutside` suppression on the chooser's DialogContent; overlay-click dismiss (its own `onPointerDown`) and Escape still close; the full entity dialogs stay modal. Unit pin added.
Full-suite runs triaged systematically (spec bugs fixed: CardTitle renders a div not a heading; strict-mode violations on toasts; goal-card assertions needed proper card scoping; fixtures needed multi-line CSV). One infrastructure lesson: crashed early runs left **~65 zombie chromium processes** starving later launches (looked like locator timeouts) — killed them, after which the suite went fully green. **66/66 twice consecutively, zero flakes (~3.4 min each). Dev DB untouched — verified by mtime + pristine counts.**
Gates at that point: lint 0 · tsc 0 · **275/275** unit (270 + 3 title + 2 import-negative) · build 0 · E2E 66/66 ×2.
Continuation session (this one): CI + cleanup + docs + delivery.
**CI workflow** (`.github/workflows/ci.yml`): checkout@v4 → setup-bun@v2 (1.3.x) → `bun install --frozen-lockfile` → `bun run db:generate` (bun blocks postinstall scripts — the Prisma client must be generated explicitly) → lint → typecheck → unit → Playwright-version-keyed browser cache → `bunx playwright install --with-deps chromium` → `bun run test:e2e`; failure-only report artifact. Validated structurally: YAML safe-parse, every referenced script exists in package.json, frozen-lockfile dry-run clean. Two CI-correctness facts verified empirically: `next build` succeeds WITHOUT `.env` (every API route is dynamic — never evaluated at build time) and the unit suite is pure (275/275 without `.env`). `db/.gitkeep` is committed, so fresh clones can create `db/e2e.db`. First remote execution happens on the push itself (the deploy key grants push only) — flagged verify-on-GitHub.
**F3 cleanup**: `tailwind.config.ts` deleted (verified referenced by nothing — the v4 CSS-first pipeline uses `@tailwindcss/postcss`; `tw-animate-css` comes via CSS import) and the now-dead `tailwindcss-animate` dependency removed (`bun remove`; frozen-lockfile still clean). Post-delete `bun run build` green.
**Docs pass**: round-12 plan §G execution record appended; AGENTS.md (commands table + E2E gate, push contract, E2E/title/ADR-024-import/FAB invariants, tailwind quirk updated, reference list); CLAUDE.md (round-12 ANALYZE ref, VERIFY gate, commands table, testing strategy — E2E layer replaces "planned next layer", round-12 contract list, tailwind line); README (CI badge, 275 counts, E2E rows in Key Features + Verify Setup, file hierarchy with `e2e/` + workflow, Testing & Verification rewritten — E2E suite + settings-independence + CI, push gate); PAD v1.11 (header, §7.1 E2E + CI rows and per-file counts, §7.2 round-12 evidence, §7.3/7.4 updated, §8.4 CI described, §10 resolved-3-items paragraph + rows removed, §11 key files). This session log.
Final gates re-run on the finished tree: lint 0 · tsc 0 · 275/275 · build 0 · E2E 66/66. Dev DB still pristine. Live state clean (settings restored and persisted after the F0 probe; theme left dark as found; no other live mutations).
Delivered: E2E layer (66 specs / 11 files / ~1024 lines), CI pipeline, 3 live-probed parity fixes (login-surface title contract + the `<title>`-hydration MutationObserver mechanism; import-path ADR-024 alignment; non-modal Quick Add chooser), settings-independent-export verification (F0, zero code change), `tailwind.config.ts` + `tailwindcss-animate` removal, suite 270 → 275 unit + 66 E2E, docs aligned (plan §G, AGENTS, CLAUDE, README, PAD v1.11, session_11). Atomic commits on main, pushed via `docs/ssh_git_wrapper_v3.py` (dry-run → push → verify → shred).
