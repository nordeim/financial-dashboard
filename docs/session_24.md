Round 19 continues from the pushed round-18 state (remote main @ 961a64f, synced with origin — the round-18 session-log follow-up commit). The workspace had been RESET (a bare initial commit, no remote) — a fresh `git clone` re-established the tree at the pushed state, and the environment was rebuilt from scratch: `bun install` (641 packages), `.env` from `.env.example`, `db:push` to the repo-local `db/custom.db`, and `bunx playwright install chromium` (the E2E runner's chromium_headless_shell-1234 was missing from the reset cache — the first E2E attempt failed 67/67 on `browserType.launch` before the install; not a code regression). The known environment trap re-surfaced immediately: the sandbox's pre-set `DATABASE_URL` shell variable (an absolute path to a non-existent parent-workspace DB) shadows the repo `.env` — handled by `unset DATABASE_URL` on every server/test invocation. The baseline gates opened green: lint 0 · tsc 0 · 389/389 unit — matching the documented round-18 state exactly.

The operating instructions were internalized (the repo skills catalog — clone-app-pat-pro / agent-browser / tdd — and the scandihaven tech-stack reference, all institutionalized since round 18). The core docs were re-read (AGENTS, CLAUDE, README, PAD v1.17, session_22, session_23, the round-18 plan) and the codebase alignment validated: 17 finara component files, 17 unit files / 389 specs, 11 E2E files / 67 specs, the route table, the round-18 seams (`route-metadata.ts` with `buildRouteViewport`/`absoluteCanonicalUrl`/`twitterDescription`, `robots.ts`, `sitemap.ts`) — the tree matched the documented round-18 state exactly.

The round opened with the standard full live re-probe (agent-browser isolated sessions; the clone side against a fresh production build on :3000, toggled to dark to theme-match the live account): login + the 9 views + login-on-logged-out captured on both sides (10 surfaces + login), then BOTH diffs (the sorted signature-multiset AND the round-16 raw-order mode) plus the structural probes and a 5-pair VLM side-by-side sweep. Verdict: **the live BODY DOM is UNCHANGED since round 18** — zero non-bucketed deltas on all 11 surfaces in both diff modes (every residual delta classifies into the documented buckets: seed-data counts, lucide svg internals, the live's AI-insight empty state, infra, the sonner toaster, a11y additions, the login demo-credentials affordance), the raw counts equal the sorted counts everywhere (no class-order drift), and every structural probe matched its pin: the motion audit (15 settled elements on the live Dashboard — a DATA-bucket delta from the pinned 19: the live user's recent-activity rows shrank to 2; the wrapper anatomy — header row, 4 KPI wrappers, 4 gradient tiles, Budget Overview, the per-budget space-y-2 row, Recent Activity, its rows, Quick Actions — is unchanged), the Add Expense modal (overlay/card classes + the settled style + the div title with the full literal), the FAB chooser (z-40 overlay + `opacity: 1;` · `w-full max-w-md` wrapper + `opacity: 1; transform: none;` · the plain card · "Quick Add" / "What would you like to add?"), the Progress indeterminate mechanism (manual translateX −75.5% at 24.5%, no aria-valuenow), the mobile chrome (the top bar + the drawer root carrying the settled style, the slide-in rAF-sampled at −300px → +36.1px overshoot → settle, 2 navs / 18 links, the `p-4 border-t border-slate-700/30` bottom with the direct Sign Out), the SyncedBadge (base-only render, mobile top bar + desktop sidebar bottom), the sidebar raw orders (aside/container/header/nav/bottom all exact), and the user-menu anatomy (Dark Mode + Sign Out items, span-wrapped labels). The VLM sweep returned EQUIVALENT on Dashboard/Expenses/Investments/Goals; the Analytics pair initially flagged on chart-scale data differences + a viewport-crop artifact (the average cards below the fold) — the full-page re-comparison returned EQUIVALENT. Net: 5/5. (One live-probe incident, recovered: the first mobile-drawer attempt clicked the top bar's FIRST button — the theme toggle — flipping the live account to light; caught via the VLM theme mismatch, toggled back to dark, the screenshots retaken dark-verified. The signature captures are class-attribute-based and theme-independent — unaffected.)

The decisive move this round — the HTTP-layer audit (the round-17 lesson, standard since then; the live head has now changed THREE rounds in a row): re-run at the curl level, it found the live's `<head>` moved AGAIN since round 18. Two findings, both live-probed and byte-verified: (F1) the round-18 `{ViewLabel} on Finara. {SITE_DESCRIPTION.slice(0, 80)}.` 98-char mid-word-truncation template now applies to the MAIN `description` and `og:description` metas TOO — probed on all 8 non-dashboard view routes; /, /Dashboard, /login and 404s keep the FULL text on all three description metas (round 18 had the template on twitter:description only; the clone rendered the FULL text on description + og:description everywhere); (F2) /login renders `twitter:image:alt "Base44 link preview"` — the clone rendered the bare twitter:image on every route (no twitter:image width/height on ANY route — the og:image dims stay og-only, the round-18 shape). The round-17/18 pins that HOLD were re-verified unchanged: the security headers (the live trio + the clone's hardening pair on pages AND api, no X-Powered-By), robots.txt (the wildcard shape), sitemap.xml (the 9-URL set), the per-route og:title/og:url/canonical (including the dashboard→root quirk), the viewport/theme-color split (login-only), the icon links (the login sized pair), the og:image shapes (the login descriptor), twitter:url/card/title, the title set, the PWA metas, the broken manifest link (still deliberately not replicated).

The round-19 remediation plan was written (docs/plans/2026-09-19-parity-remediation-round19.md) and validated against the codebase BEFORE execution — the breakage surface confirmed to be exactly `site-metadata.test.ts` (no other spec or E2E pins the description metas or the twitter image alt), and the F2 implementation surface confirmed in the framework source (Next renders `twitter:image:alt` from the twitter images' `alt` field — `node_modules/next/dist/lib/metadata/metadata.js` — so no escape hatch was needed).

TDD execution — RED first: the re-pinned + new site-metadata specs failed exactly as designed (3 failing / 389 passing). GREEN: both findings implemented (F1 the seam's private `twitterDescription` generalized to `routeDescription` wired into all three description fields — one per-route string everywhere; F2 the login twitter image alt). No implementation corrections were needed — tsc passed first try (the round-18 `"card" in twitter` narrowing lesson held).

Post-fix verification on the fresh production build: /Goals + /Income render the template byte-identical to the live on all three description metas (98/99 chars, compared programmatically); /login renders twitter:image:alt "Base44 link preview" with no twitter:image dims; the round-17/18 pins re-verified. The body-DOM regression check: the clone signature diff pre-fix vs post-fix is BYTE-IDENTICAL (525/525 on the Dashboard root, 268/268 on Goals, both modes) — the round moved only the transport/metadata layer.

Gates (final): lint 0 · tsc 0 · unit 392/392 (+3 net-new) · build clean · E2E 67/67 (after the chromium install). Deliverables beyond the fixes: AGENTS/CLAUDE/README/PAD v1.18 aligned (the per-route-description invariant, the round-19 references, the counts 389 → 392, the §10 round-19 paragraph, the §11 seam entries); `docs/screenshots/` refreshed (dev-server captures of the remediated app); `.env.example` re-verified aligned (no new env vars — `NEXT_PUBLIC_SITE_URL` carries the same role).

Round 19 complete — the live body DOM re-verified at zero drift (structurally unchanged since round 17), and the transport/metadata layer advanced again: the per-route description template now covers all three description metas (one per-route string everywhere) and the login route's twitter:image alt. The seams: `routeDescription` in `route-metadata.ts` (the generalized `twitterDescription`) + the login twitter image descriptor. The standing lesson, now three rounds deep: the live's `<head>` is the most volatile parity surface — the per-route head audit stays in the standard opener, and the head-structure pins live in one spec file for exactly this reason.
