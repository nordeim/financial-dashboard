Round 18 continues from the pushed round-17 state (remote main @ 35ccc34, synced with origin — the round-17 session-log follow-up commit). The workspace was intact from round 17 (only a `git pull` needed, bringing in `docs/session_21.md`), and the environment held: `bun install` state, `db/custom.db`, and the Playwright chromium cache all survived; the baseline gates opened green (lint 0 · tsc 0 · 378/378 unit — matching the documented round-17 state exactly). One recurring environment trap re-surfaced during the round: the sandbox's pre-set `DATABASE_URL` shell variable (an absolute path to a non-existent parent-workspace DB) shadowed the repo `.env` for the first production-server start — the known round-17 issue — resolved by restarting the server with the repo-relative value (`file:../db/custom.db`); the first clone-side capture pass caught it through the Dashboard's error-state card (zero API responses), and the pass was retaken against the healthy DB.

The operating instructions were internalized (the repo skills catalog — clone-app-pat-pro / agent-browser / tdd — and the scandihaven tech-stack reference: integer minor units, Tailwind v4 CSS-first, strict TS, TDD seams, idempotent seeding — all institutionalized here). The core docs were re-read (AGENTS, CLAUDE, README, PAD v1.16, session_20, session_21, the round-17 plan) and the codebase alignment validated: 17 finara components, 17 unit files / 378 specs, 11 E2E files / 67 specs, the route table, the round-17 seams (`route-metadata.ts`, `robots.ts`, `sitemap.ts`, the cleaned `public/`) — the tree matched the documented round-17 state exactly.

The round opened with the standard full live re-probe (agent-browser isolated sessions; the clone side against a fresh production build on :3000, toggled to dark to theme-match the live account): login + the 9 views captured on both sides, then BOTH diffs (the sorted signature-multiset AND the round-16 raw-order mode) plus the structural probes and a 5-pair VLM side-by-side sweep. Verdict: **the live BODY DOM is UNCHANGED since round 17** — zero non-bucketed deltas on all 10 surfaces in both diff modes (every residual delta classifies into the documented buckets: seed-data counts, lucide svg internals, the live's AI-insight empty state, infra, the duplicated sonner toaster, a11y additions, the login demo-credentials affordance), the raw counts equal the sorted counts everywhere (no class-order drift), and every structural probe matched its pin: the motion audit (19 settled elements on the Dashboard), the Add Expense modal (overlay/card classes + the settled style), the FAB chooser (z-40 overlay + `opacity: 1` · `w-full max-w-md` wrapper + `opacity: 1; transform: none` · the plain card · "Quick Add" / "What would you like to add?"), the Progress indeterminate mechanism (manual translateX −75.5% at 24.5%, no aria-valuenow), the mobile chrome (the top bar + the drawer root carrying the settled style, the slide-in rAF-sampled at −300px → +35.5px overshoot → settle), the SyncedBadge base-only render, the sidebar raw orders (container/header/navs/bottoms/pane), and the user-menu anatomy (sun + log-out items, span-wrapped labels). The VLM sweep returned EQUIVALENT on Dashboard/Expenses/Investments/Analytics; the Goals pair flagged only the goal-card count (1 live vs 3 seeded — the data bucket).

The decisive move this round: the HTTP-layer audit — the round-17 lesson, now part of the standard opener — was re-run at the curl level, and it found the live's `<head>` **RESTRUCTURED per route since round 17**. Five findings, all live-probed: (F1) the viewport/theme-color pair (`viewport-fit=cover` + `theme-color #000000`) moved to the LOGIN route only — app pages (/, /Dashboard, the views, 404s) render the plain viewport with neither field; (F2) the icon links split per route — /login renders `icon sizes="any"` + `apple-touch-icon sizes="180x180"` while app pages render a single icon link carrying `type="image/svg+xml"` (a base44 mislabel on a PNG href — deliberately NOT replicated, the same class as the broken manifest); (F3) the og:image dims/alt became login-only — app pages render a BARE og:image, /login adds width 1200/height 630/alt "Base44 link preview" (the clone declares its own asset's honest 480×480 — asset values are deployment properties, structure is the contract, per the round-17 sitemap precedent); (F4) the twitter block — the live renders twitter:title/card/bare-image (no dims) on every route plus `twitter:url` (= the absolute canonical, the BARE origin for the dashboard) and a per-route description: the FULL text on /, /Dashboard, /login and 404s but `{ViewLabel} on Finara. {SITE_DESCRIPTION.slice(0, 80)}.` — a 98-char mid-word truncation — on the eight non-dashboard view routes (probed on all 8; the clone had been rendering Next's derived block: the full description everywhere, an image WITH dims, and no url); (F5) the live head carries NO keywords meta (the clone's layout rendered one). The round-17 pins that HOLD were re-verified unchanged: the security headers (the live trio + the clone's hardening pair on pages AND api), robots.txt (the wildcard shape), sitemap.xml (the 9-URL set), the per-route og:title/og:url/canonical (including the dashboard→root quirk), the description text, og:site_name/type, and the PWA metas.

The round-18 remediation plan was written (docs/plans/2026-09-19-parity-remediation-round18.md) and validated against the codebase BEFORE execution — `generateViewport` support confirmed in the Next docs, the `Viewport`/`IconDescriptor` types confirmed to carry `viewportFit`/`themeColor`/`sizes`, Next's Twitter metadata type confirmed to have NO url field (the `metadata.other` escape hatch confirmed to render arbitrary metas from the framework source), and the breakage surface confirmed to be exactly `site-metadata.test.ts` (no other spec or E2E pins the layout viewport/keywords/twitter).

TDD execution — RED first: the re-pinned + new site-metadata specs failed exactly as designed (14 failing / 375 passing). GREEN: all five findings implemented (F1 `buildRouteViewport` in the seam + the layout `viewport` slimmed + the catch-all page's `generateViewport` export; F4 the explicit per-route `twitter` block + `twitterDescription` + `other["twitter:url"]` + `absoluteCanonicalUrl` with `siteBaseUrl` trailing-slash normalization; F3 the per-route og:image shapes + the layout default slimmed; F2 the layout `icons` dropped apple + the seam's login-level sized icon pair; F5 the keywords field removed). One correction caught by tsc during GREEN: Next's `Twitter` type is a union whose base member carries no `card` — the spec narrows with `"card" in twitter` before reading it.

Post-fix verification on the fresh production build: the app pages render the plain viewport, a single bare icon link, the bare og:image, the byte-exact twitter description template on /Goals ("Goals on Finara. … bring clarity and con."), twitter:url at the absolute canonical (the bare origin for the dashboard), and no keywords meta; /login renders viewport-fit=cover + theme-color #000000, the sized icon pair, the og:image descriptor (480×480 + "Base44 link preview"), and the full 274-char twitter description; the five security headers and the robots/sitemap shapes re-verified. The body-DOM regression check: the clone Dashboard signature diff pre-fix vs post-fix is BYTE-IDENTICAL (179/179 both modes) — the round moved only the transport/metadata layer.

Gates (final): lint 0 · tsc 0 · unit 389/389 (+11 net-new) · build clean · E2E 67/67. Deliverables beyond the fixes: AGENTS/CLAUDE/README/PAD v1.17 aligned (the per-route head-structure invariant, the round-18 references, the counts 378 → 389, and the PAD §7 stale counts fixed — 348/16 files and 311 → 389 — plus the §10 round-18 paragraph and the §11 seam entries); `docs/screenshots/` refreshed (dev-server captures of the remediated app); `.env.example` re-verified aligned (no new env vars — `NEXT_PUBLIC_SITE_URL` carries the same role).

Round 18 complete — the live body DOM re-verified at zero drift (the live structurally unchanged since round 17), and the transport/metadata layer advanced from "the shared head matches" to "the PER-ROUTE head structure matches": the login route's richer head block, the app pages' slimmed head, the explicit twitter block with its per-route description template and twitter:url, and the removed keywords meta. The new seams: `buildRouteViewport` + `absoluteCanonicalUrl` in `route-metadata.ts`, the catch-all's `generateViewport`, and the `metadata.other` escape hatch — with the "Next's Twitter type has no url field" lesson recorded in the plan.
