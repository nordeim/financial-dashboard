Round 17 continues from the pushed round-16 state (remote main @ c759c7d, synced with origin — the round-16 session-log commit). The workspace had been reset, so the round opened with a fresh `git clone` and environment rebuild; one environment fix worth recording: the sandbox's pre-set `/home/z/my-project/.env` (an absolute `DATABASE_URL`) shadowed the repo's `.env` through the exported shell variable, sending the first `db:push` to the wrong location — unset the variable, removed the shadow file, and the DB landed repo-local (`db/custom.db`) as documented.

The operating instructions were internalized (coding_agent_prompt.md, the repo skills catalog — clone-app-pat-pro / agent-browser / tdd — and the scandihaven tech-stack reference: integer minor units, Tailwind v4 CSS-first, strict TS, TDD seams, idempotent seeding — all already institutionalized here). The core docs were re-read (AGENTS, CLAUDE, README, PAD v1.15, session_18, session_19, the round-16 plan) and the codebase alignment validated: 16 finara components, 16 unit files / 359 specs, 11 E2E files / 67 specs, the route table, the pinned primitives — the tree matched the documented round-16 state exactly. Baseline gates green first: lint 0 · tsc 0 · 359/359 unit (after `bun install`, `cp .env.example .env`, `db:generate` + `db:push`; the Playwright chromium cache was wiped with the reset — reinstalled before the E2E gate, the known round-16 lesson).

The round opened with the standard full live re-probe (agent-browser isolated sessions; the clone side against a fresh production build on :3000, toggled to dark to theme-match the live account): login + the 9 views captured on both sides, then BOTH diffs (the sorted signature-multiset AND the round-16 raw-order mode). Verdict: **the live app is UNCHANGED since round 16** — zero non-bucketed deltas on all 10 surfaces, and the raw counts equal the sorted counts everywhere (no class-order drift). Every residual delta classifies into the documented buckets (seed-data counts, lucide svg internals — the live's old lucide renders polylines where the clone's renders paths, the AI-insight empty state vs records, infra, the duplicated sonner toaster, a11y additions, the login demo-credentials affordance). One live-side change: **the live ACCOUNT DATA was reset** — the account now holds a minimal set (1 expense "Coffee" $4.50, 1 income source, 1 account, 1 goal, 1 budget row, ~1-2 holdings), verified directly on the live; a data difference, not structure. The structural probes all matched the round-14/16 pins: the motion audit (19 settled elements on the Dashboard), the Add Expense modal (overlay/card classes + settled style), the FAB chooser (overlay > wrapper > card), the Progress indeterminate mechanism (manual translateX, no aria-valuenow), the mobile chrome (top bar, drawer slide-in rAF-sampled at −300px → +35px overshoot → settle ~590ms — matching the round-16 `fin-drawer-in` keyframe), the SyncedBadge base-only render, the sidebar raw orders, and the user-menu anatomy. A 5-pair VLM side-by-side sweep (dark/dark) returned EQUIVALENT on Dashboard/Expenses/Investments/Analytics; the Goals pair flagged only the goal-card count (1 live vs 3 seeded — the data bucket).

The decisive move this round: with body-DOM parity holding and no new security drift (`bun audit` identical to the round-15 accepted tail — 43 findings, 0 critical, all dev/CLI-time), the probe extended to the **HTTP transport and document-metadata layer** the DOM diff is structurally blind to (curl-level): response headers, the `<head>` metadata, robots.txt, sitemap.xml, and the favicon assets. Five findings, all live-probed: (F1) the live's edge sends `Referrer-Policy: strict-origin-when-cross-origin`, `X-Content-Type-Options: nosniff`, and `Strict-Transport-Security: max-age=31536000` on every response — the clone sent none and additionally leaked `X-Powered-By: Next.js`; (F2) the live's head carries per-route og:title/og:url (mirroring the document titles — bare `Finara` on /, /Dashboard, /login; `Goals | Finara` on views; even the 404 route camelCase-splits), with the DASHBOARD canonical normalizing to `/` (a live quirk), plus theme-color `#000000`, `viewport-fit=cover`, its exact description text, og:image (the logo), and the apple-mobile-web-*/mobile-web-app-capable PWA metas — the clone's head diverged on every one of those fields; (F3) the live's favicon IS the Finara logo — the clone's was a generic emerald SVG mark (and `finara-logo.png` was a JPEG mislabeled with a .png extension, plus a dead scaffold `logo.svg`); (F4) the per-route metadata needed a pure seam (the page files pull the client React graph — not unit-importable); (F5) the live's robots.txt is the wildcard shape (`User-agent: *` + `Allow: /` + a `Sitemap:` line — not the clone's per-bot list) and its sitemap.xml lists exactly 9 URLs (`/` 1.0 + the eight non-dashboard views 0.8/weekly; no `/Dashboard`, no `/login`) — the clone's /sitemap.xml fell through the catch-all to the NotFoundView HTML. The live's `manifest.json` link 302s to nothing (a broken base44 asset — deliberately NOT replicated).

The round-17 remediation plan was written (docs/plans/2026-09-19-parity-remediation-round17.md) with the five findings and validated against the codebase BEFORE execution — next.config.ts importability in Vitest verified with a throwaway spec, the PIL PNG conversion verified pixel-identical, the parseRoute/AppRoute types checked against the seam design, and no existing test found referencing robots/sitemap/headers/metadata (no breakage surface).

TDD execution — RED first: `src/lib/__tests__/site-metadata.test.ts` (the five finding contracts) failed at import exactly as designed (the seam module did not exist); 359 pre-existing green. GREEN: all five findings implemented (F1 next.config.ts headers + poweredByHeader off; F4 `src/lib/route-metadata.ts` — `buildRouteMetadata` + `siteBaseUrl` + `SITE_DESCRIPTION` — consumed by both page files; F2 layout.tsx viewport export + description + icons + appleWebApp + metadataBase + openGraph defaults; F5 `app/robots.ts` + `app/sitemap.ts` + `public/robots.txt` deleted; F3 the logo converted to a true PNG + the dead assets removed). Two corrections caught during GREEN, both by the gates: tsc flagged Next's sitemap field as `changeFrequency` (not `changefreq`), and the first curl verification showed og:image/og:site_name/og:type MISSING — Next merges metadata SHALLOWLY per key, so the page-level openGraph had silently REPLACED the layout's; the seam was fixed to return the COMPLETE openGraph block and a new spec pinned that behavior (the shallow-merge trap is now recorded in AGENTS).

Post-fix verification on the fresh production build: the security headers render on `/login` AND `/api/settings` (the live trio + X-Frame-Options DENY + Permissions-Policy), `X-Powered-By` gone; robots.txt/sitemap.xml match the live shapes (application/xml, 9 URLs); the per-route head renders title/og/canonical live-exactly on /Dashboard (canonical root), /login, /Goals, and /NonexistentPage; theme-color, viewport-fit, the description, og:image, the PWA metas, and the logo favicon all present. The body-DOM regression check: the clone Dashboard signature diff pre-fix vs post-fix is BYTE-IDENTICAL (179/179 both modes) — the round moved only the transport/metadata layer.

Gates (final): lint 0 · tsc 0 · unit 378/378 (+19) · build clean · E2E 67/67. Deliverables beyond the fixes: `.env.example` documents `NEXT_PUBLIC_SITE_URL`; `docs/screenshots/` refreshed (dev-server captures of the remediated app); AGENTS/CLAUDE/README/PAD v1.16 aligned (the HTTP-layer parity invariant, the round-17 references, counts 359 → 378, the security-headers row, the env tables, and the stale CI counts fixed 275 → 378 / 66 → 67).

Round 17 complete — body-DOM parity re-verified at zero drift (the live structurally unchanged since round 16; its account data reset), and the parity discipline extended to the transport/metadata layer it was structurally blind to: security headers, per-route head metadata, favicon identity, and the SEO files all live-exact now, with the "audit the HTTP layer separately" lesson added to the permanent workflow.
